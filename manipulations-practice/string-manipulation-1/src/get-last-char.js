/* exported getLastChar */
// function with string as argument
// return last character of string - string.length - 1

function getLastChar(string) {
  return string[string.length - 1]; // returns expression string with string at i index; i being string.length - 1 or the last character in the string
}
