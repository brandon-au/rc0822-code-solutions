/* exported setValue */

// function named setValue with object, key, and value as parameters
//

function setValue(object, key, value) {
  object[key] = value; // use bracket notation to set key at object equal to the value
}
